GEMINI_API_KEY=
MONGODB_URI=mongodb://localhost:27017/km_resume
PORT=4000

# Copy this file to `.env` and fill the values. Do NOT commit your `.env`.# IMPORTANT: Copy this file to .env (do NOT commit .env to git).
# Replace the values with your real credentials.

# Gemini API Key from Google AI Studio: https://aistudio.google.com/app/apikey
# This is required. Get your key from the link above.
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# MongoDB connection string (for persisting chat sessions)
# Default: local MongoDB on standard port
MONGODB_URI=mongodb://localhost:27017/km_resume

# Server port (default: 4000)
PORT=4000

# Optional: Service token to protect /api endpoints from unauthorized requests
SERVICE_TOKEN=some_random_secret_token_here
